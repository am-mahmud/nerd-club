import LoginPage from '@/components/login';
import React from 'react';

const page = () => {
    return (
        <div className='h-screen'>
            <title>Login</title>
            <LoginPage></LoginPage>
        </div>
    );
};

export default page;